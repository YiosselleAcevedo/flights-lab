<!DOCTYPE html>
<html>

<head>
  <title>
    <%= title %>
  </title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

</head>

<body class="p-3 mb-2 bg-light text-dark" .bg-light>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
    crossorigin="anonymous"></script>

  <span>Gundam: <%= title %> </span><a href="../">HOME</a>

  <h4>Here is your Gundam </h4>


  <ul class="list-group">
    <li class="list-group-item list-group-item-dark">
      <%= gundam.name %>
    </li>
    <li class="list-group-item list-group-item-dark">
      <%= gundam.grade %>
    </li>
    <li class="list-group-item list-group-item-dark">
      <%= gundam.rating %>
    </li>
    <li class="list-group-item list-group-item-dark">
      <%= gundam.gundamtype %>
    </li>
  </ul>

  <h5>And here are your parts so far: </h5>
  <ul class="list-group">
    <% parts.forEach(t=> { %>
      <li class="list-group-item list-group-item-dark">
        <%= t.name %> - <%= t.price %>
      </li>
      <% }) %>
  </ul>
  <hr />


  <h4>Need to make an update:</h4>
  <form action="/gundam/<%= gundam._id %>" method="post">
    <input type="hidden" name="_id" value="<%= gundam._id %>">
    <div class="mb-3">
      <label class="form-label">Name</label>
      <input type="text" name="name" value="<%= gundam.name %>" class='form-control' placeholder='Name...'>
    </div>
    <div class="mb-3">
      <label class="form-label">Grades</label>
      <select name="grade" class="form-select form-select-sm" aria-label=".form-select-sm" >
        <%
        var options = [ "Standard", "High", "Metal", "Perfect" ];
        for ( var i = 0; i < options.length; i++ )
        {
            var selected = ( gundam.grade == options[i] ) ? "selected" : "";
            %><option value="<%=options[i] %>" <%=selected %>><%= options[i] %></option><%
        }
        %>
      </select>
    </div>

    <div class="mb-3">
      <label class="form-label">Rating</label>
      <select name="rating" class="form-select form-select-sm" aria-label=".form-select-sm">
        <%
        var options = [ "1/144", "1/100", "1/60", "1/48", "1/35" ];
        for ( var i = 0; i < options.length; i++ )
        {
            var selected = ( gundam.rating == options[i] ) ? "selected" : "";
            %><option value="<%=options[i] %>" <%=selected %>><%= options[i] %></option><%
        }
        %>
      </select>
    </div>

    <div class="mb-3">
      <label class="form-label">Type</label>
      <select name="gundamtype" class="form-select form-select-sm" aria-label=".form-select-sm" value="<%= gundam.gundamtype %>">

        <%
        var options = [ "EXIA", "RX78", "UNICORN", "GOD", "ASTRAY RED" ];
        for ( var i = 0; i < options.length; i++ )
        {
            var selected = ( gundam.gundamtype == options[i] ) ? "selected" : "";
            %><option value="<%=options[i] %>" <%=selected %>><%= options[i] %></option><%
        }
        %>
      </select>
    </div>
    <div class="mb-3">
      <input type="submit" value="Update Gundam" class="btn btn-primary">
    </div>
  </form>
</body>

</html>